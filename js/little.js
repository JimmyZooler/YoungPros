/* 
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
//document.addEventListener("DOMContentLoaded", function(event) { 
//  
//
//  document.getElementById("post-50").onmouseenter = addMyClass('translate-y-ten');
//  document.getElementsByClassName("post").onmouseleave = removeClass(this,"translate-y-ten");
//
//    function addMyClass(cls) {
//      if (!hasClass(this, cls)) {
//            var classString = this.className; // returns the string of all the classes for myDiv
//            var newClass = classString.concat(" " + cls); // Adds the class "main__section" to the string (notice the leading space)
//            this.className = newClass; // sets className to the new string
//        }
//    }
//    
//    function hasClass(ele,cls) {
//        return !!ele.className.match(new RegExp('(\\s|^)'+cls+'(\\s|$)'));
//      }
//    
//    function removeClass(ele,cls) {
//        if (hasClass(ele,cls)) {
//          var reg = new RegExp('(\\s|^)'+cls+'(\\s|$)');
//          ele.className=ele.className.replace(reg,' ');
//        }
//    }
//});